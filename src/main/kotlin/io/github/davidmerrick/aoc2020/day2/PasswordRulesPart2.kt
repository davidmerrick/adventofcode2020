package io.github.davidmerrick.aoc2020.day2

/**
 * Assumes indices are zero-indexed,
 * so normalize these first
 */
data class PasswordRulesPart2(
    val inputCharacter: Char,
    val lowerIndex: Int,
    val upperIndex: Int
)
